// Tailwind CSS 4.x uses the @tailwindcss/postcss plugin and a CSS-first
// configuration model. See src/styles.scss for the @import "tailwindcss" line.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
