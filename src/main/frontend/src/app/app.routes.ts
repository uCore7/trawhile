import { Routes } from '@angular/router';

/**
 * Application routes. Feature routes are added by the cluster-specific
 * Phase 7 frontend implementation tasks; this baseline is intentionally empty.
 */
export const routes: Routes = [];
