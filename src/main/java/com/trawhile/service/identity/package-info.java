/**
 * Identity and access cluster (architecture §5.2.2): user identity (Account
 * service) and the delegated-access constructs (Authorization service — node
 * grants, API keys, webhook subscriptions). Reachable from Web and the Auth
 * flow adapter.
 */
package com.trawhile.service.identity;
